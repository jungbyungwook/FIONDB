import { FC, ReactNode } from 'react';
/*
import styled from 'styled-components';

interface FlexItemProps {
  children?: ReactNode;
  style?: {
    flex: number;
  };
}

const FlexItem = ({ children, style }: FlexItemProps) => {
  return (
    <StyleFlexItem flex={style?.flex ? style.flex : undefined}>
      {children}
    </StyleFlexItem>
  );
};

const StyleFlexItem = styled.div<{ flex?: number }>`
  width: 100%;
  height: 100%;
  text-align: center;
  flex: ${({ flex }) => (flex ? flex : 1)};
  border: 1px solid black;
  overflow: hidden;
`;

export default FlexItem;
*/
